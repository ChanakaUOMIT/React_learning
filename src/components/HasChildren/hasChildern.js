import React from 'react';


const HasChildren = (props)=>{
    // console.log(props);
    // console.log(props.children);
    return(
        <div>
            {props.children}
        </div>
    )
}
 
export default HasChildren;  